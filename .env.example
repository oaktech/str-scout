# Database
# Set DATABASE_URL for PostgreSQL (production/Railway).
# Leave unset to use SQLite locally (no setup needed).
# DATABASE_URL=postgresql://user:password@localhost:5432/str_scout

# Anthropic API (for document extraction)
ANTHROPIC_API_KEY=sk-ant-...

# Google Places API (for address autocomplete — optional)
# VITE_GOOGLE_PLACES_API_KEY=AIza...

# Server
PORT=3001
NODE_ENV=development
